#include<stdio.h>

int main()
{
       int a=10;
       int b=5;
       printf("the sum of two numbers a and b is %d\n ",a+b);
       printf("the difference of two numbers a and b is %d\n ",a-b);
       printf("the product of two numbers a and b is %d\n ",a*b);
       printf("the modulus of two numbers a and b is %d\n ",a/b);
       return 0;
}
